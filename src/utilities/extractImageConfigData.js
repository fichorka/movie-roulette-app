// extractImageConfigData
export default (
  {
    secure_base_url: baseUrl,
    backdrop_sizes: backdropSizes,
    poster_sizes: posterSizes
  }
) => (
  {
    baseUrl,
    backdropSizes,
    posterSizes
  }
)
